post_install() {
	post_upgrade
}

post_upgrade() {
	systemctl enable vnstat.service
	# Arch ships only the arpwatch@.service template (no plain arpwatch.service);
	# eth0 matches the documented default in docs/networking/arpwatch.md.
	systemctl enable arpwatch@eth0.service
}
